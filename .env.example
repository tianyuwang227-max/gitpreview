# GitHub Personal Access Token (optional, increases API rate limit)
GITHUB_TOKEN=

# Base directory for cloned projects
CLONE_BASE_DIR=./projects

# Clone timeout in milliseconds
CLONE_TIMEOUT=60000

# Server port (for future web UI)
PORT=3000
